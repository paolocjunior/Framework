# Local settings (session-specific permissions)
.claude/settings.local.json

# Runtime session data (generated during use)
.claude/runtime/session-summaries/latest.md

# Runtime gate markers (created by commands, consumed by hooks)
.claude/runtime/.plan-approved

# OS files
.DS_Store
Thumbs.db

# Node (if docx generation is run locally)
node_modules/
package.json
package-lock.json

# Temp files
*.tmp
/tmp/
